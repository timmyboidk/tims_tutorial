server {
    listen 80;
    server_name localhost;
    
    # 💡 我们的 React 静态打包文件都会存放在这里
    root /usr/share/nginx/html;
    index index.html;

    # 💡 SPA 的灵魂配置：因为前端用了 React Router，如果在子路径上直接刷浏览器，
    # Nginx 默认会去找真实的 HTML 导致 404。此规则确保一切未知请求全权交回 index.html。
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 💡 让 Nginx 承担网关职责：凡是带 /api 前缀的请求，统统发给内网的 API 容器处理
    # 这完全复刻了前端 Vite 在 package.json 里 dev:proxy 的行为！
    location /api/ {
        # 注意：这里的 'api' 指的是 docker-compose.yml 里面给 Spring Boot 容器起的名字
        proxy_pass http://api:8080/api/;
        
        # 将原始请求的 Host 和 IP 带着
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
